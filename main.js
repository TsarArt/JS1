'use strict';
// 1)
const tempC = prompt('Введите пожалуйста температуру в цельсиях для переобразования в температуру по фаренгейту!');
const tempF = (9 / 5) * tempC + 32;

// 2)
const name = prompt('Введите имя!');
const admin = name;

// 3) *Чему будет равно JS-выражение 1000 + "108"? Будет равно "1000108" из-за того, что 1000 будет преобразована к строке

// 4)
// async - страница не ждет скрипты и показывает все по мере загрузки
// defer - все загружает и только потом показывает страницу